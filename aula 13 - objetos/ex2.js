// 2. Add and change
// Add 'toy' to the cat object.
// Change the 'age' to another number.


let cat = {
    name: "Salem",
    color: "black",
    age: 7,
}

cat.toy = "Pooh bear"
cat.age = 8

console.log(`My cat ${cat.name} is ${cat.age} years old, her color is ${cat.color} and her favorite toy is the ${cat.toy}.`)