// 1. Simple object
// Make an object 'cat' with 'name', 'color', and 'age'.
// Show the values in the console.

let cat = {
    name: "Salem",
    color: "black",
    age: 7
}

console.log(`My cat ${cat.name} is ${cat.age} years old and her color is ${cat.color}`)